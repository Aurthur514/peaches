# Example Streamlit secrets (TOML format)
# Do NOT store real keys in the repo — use Streamlit Cloud Secrets to keep them private.

DB_USERNAME = "myuser"
DB_TOKEN = "abcdef"

[some_section]
some_key = 1234

# Add your LLM keys here (do NOT commit real keys):
# GEMINI_API_KEY = "<your_gemini_key>"
# OPENROUTER_API_KEY = "<your_openrouter_key>"
# OPENAI_API_KEY = "<your_openai_key>"
