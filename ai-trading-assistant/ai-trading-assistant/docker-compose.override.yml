version: '3.8'
services:
  backend:
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8000
  frontend:
    volumes:
      - ./frontend:/app/frontend
    ports:
      - "3000:80"
